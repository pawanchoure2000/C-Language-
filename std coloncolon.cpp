#include<iostream>		//c++ program with c++ header 
#include<cmath>		//c++program with  c header   //but cannot write c program with c++ header
int main()
{
	int a=3,b=2;
	int sum;
	sum=a+b;
	std::cout<<sum;
	int sq=std::sqrt(sum);
	std::cout<<sq;
	
}
